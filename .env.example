# Optional: turns on the assistant's smart mode (a Claude model designs the icons).
# Create a key at https://console.anthropic.com/ and add it here for local dev
# (.env.local) or in Vercel > Project > Settings > Environment Variables.
ANTHROPIC_API_KEY=

# Optional: which Claude model smart mode uses (default: claude-opus-5).
ASSISTANT_MODEL=
